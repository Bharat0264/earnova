import mongoose from 'mongoose'
const actionSchema = new mongoose.Schema({ type: String, label: String, externalMutation: { type: Boolean, default: false }, status: { type: String, default: 'PENDING' } }, { _id: false })
const schema = new mongoose.Schema({ business: { type: mongoose.Schema.Types.ObjectId, ref: 'Business', required: true, index: true }, capabilityKey: { type: String, required: true }, verificationRun: { type: mongoose.Schema.Types.ObjectId, ref: 'VerificationRun' }, diagnosis: { type: mongoose.Schema.Types.Mixed, default: {} }, proposedActions: [actionSchema], status: { type: String, enum: ['PROPOSED','AWAITING_CONFIRMATION','APPROVED','EXECUTING','COMPLETED','FAILED','REJECTED','EXPIRED'], default: 'AWAITING_CONFIRMATION' }, requiresConfirmation: { type: Boolean, default: true }, requestedBy: { type: mongoose.Schema.Types.ObjectId, ref: 'User', required: true }, approvedBy: { type: mongoose.Schema.Types.ObjectId, ref: 'User' }, approvedAt: Date, executedAt: Date, result: String }, { timestamps: true })
schema.index({ business: 1, createdAt: -1 }); export default mongoose.model('RecoveryPlan', schema)
