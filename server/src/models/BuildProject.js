import mongoose from 'mongoose'
const schema = new mongoose.Schema({ user: { type: mongoose.Schema.Types.ObjectId, ref: 'User', required: true, index: true }, business: { type: mongoose.Schema.Types.ObjectId, ref: 'Business', index: true }, projectType: { type: String, required: true, trim: true }, requirements: { businessName: String, existingDomain: String, features: [String], notes: String, budgetRange: String, timeline: String }, selectedPackage: { type: String, enum: ['starter', 'business', 'premium', 'request_quote'], default: 'request_quote' }, quote: { type: mongoose.Schema.Types.Mixed }, status: { type: String, enum: ['DRAFT','REQUIREMENTS_RECEIVED','QUOTED','PAYMENT_PENDING','IN_PROGRESS','REVIEW','DEPLOYED','COMPLETED','CANCELLED'], default: 'REQUIREMENTS_RECEIVED', index: true }, paymentStatus: { type: String, default: 'NOT_STARTED' }, domainStatus: { type: String, default: 'NOT_STARTED' }, deploymentStatus: { type: String, default: 'NOT_STARTED' } }, { timestamps: true })
schema.index({ business: 1, createdAt: -1 }); schema.index({ user: 1, createdAt: -1 })
export default mongoose.model('BuildProject', schema)
