import Business from '../models/Business.js'
import Product from '../models/Product.js'
export const getStore=async(req,res)=>{const business=await Business.findOne({slug:req.params.slug,status:'active'}).select('name slug description location verificationStatus logo').lean();if(!business)return res.status(404).json({success:false,message:'Store not found.'});const products=await Product.find({business:business._id,isActive:true,published:true}).select('-reviews.user').lean();const ratings=products.filter(p=>p.rating>0);res.json({success:true,store:{name:business.name,slug:business.slug,description:business.description,location:business.location,logo:business.logo,verified:business.verificationStatus==='verified',rating:ratings.length?ratings.reduce((s,p)=>s+p.rating,0)/ratings.length:undefined,products}})}
