import {Router} from 'express';import{protect}from '../middleware/auth.js';import{profile,saveProfile,eligible,rfq,quotes,submit,withdraw}from '../controllers/supplierController.js';const router=Router();router.use(protect);router.route('/profile').get(profile).put(saveProfile);router.get('/rfqs',eligible);router.get('/rfqs/:rfqId',rfq);router.post('/rfqs/:rfqId/quotes',submit);router.get('/quotes',quotes);router.post('/quotes/:quoteId/withdraw',withdraw);export default router
