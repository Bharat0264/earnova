import { IListing } from "@/utils/types";
export default function ListingCard({item,type}:{item:IListing;type:"product"|"service"}){const price=type==="service"?item.startingPrice:item.price; return <article className="rounded-xl border bg-white p-4 shadow-sm"><p className="mb-2 text-xs font-semibold text-blue-600">{item.category}</p><h3 className="text-lg font-semibold">{item.title}</h3><p className="mt-2 text-sm text-slate-600">{item.description}</p><div className="mt-4 flex items-center justify-between"><span className="font-bold">INR {price}</span><button className="rounded-lg bg-blue-600 px-3 py-1.5 text-sm text-white">View</button></div></article>;}
