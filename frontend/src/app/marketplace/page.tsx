"use client";
import { useEffect,useState } from "react"; import ListingCard from "@/components/ListingCard"; import api from "@/services/api"; import { IListing } from "@/utils/types";
const categories=["","Electronics","Solar Panels","Design","Development","Video Editing"];
export default function MarketplacePage(){const [items,setItems]=useState<IListing[]>([]); const [q,setQ]=useState(""); const [category,setCategory]=useState(""); const [loading,setLoading]=useState(true); useEffect(()=>{const load=async()=>{setLoading(true); try{const {data}=await api.get("/products",{params:{q,category}}); setItems(data.products||[]);}finally{setLoading(false);}}; load();},[q,category]); return <section className="space-y-6"><h1 className="text-3xl font-bold">Product Marketplace</h1><div className="grid gap-3 rounded-xl border bg-white p-4 md:grid-cols-3"><input className="rounded-lg border px-3 py-2" placeholder="Search products" value={q} onChange={(e)=>setQ(e.target.value)}/><select className="rounded-lg border px-3 py-2" value={category} onChange={(e)=>setCategory(e.target.value)}>{categories.map((c)=><option key={c} value={c}>{c||"All Categories"}</option>)}</select></div>{loading?<p>Loading products...</p>:<div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{items.map((item)=><ListingCard key={item._id} item={item} type="product"/>)}</div>}</section>;}
