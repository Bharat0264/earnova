"use client";
import { useEffect,useState } from "react"; import ListingCard from "@/components/ListingCard"; import api from "@/services/api"; import { IListing } from "@/utils/types";
export default function ServicesPage(){const [services,setServices]=useState<IListing[]>([]); const [q,setQ]=useState(""); const [loading,setLoading]=useState(true); useEffect(()=>{const load=async()=>{setLoading(true); try{const {data}=await api.get("/services",{params:{q}}); setServices(data.services||[]);}finally{setLoading(false);}}; load();},[q]); return <section className="space-y-6"><h1 className="text-3xl font-bold">Freelance Services</h1><input className="w-full max-w-md rounded-lg border px-3 py-2" placeholder="Search services" value={q} onChange={(e)=>setQ(e.target.value)}/>{loading?<p>Loading services...</p>:<div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{services.map((item)=><ListingCard key={item._id} item={item} type="service"/>)}</div>}</section>;}
